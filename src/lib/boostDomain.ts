// Re-export do domínio central do configurador de boost — mantém o alias
// `@/lib/boostDomain` para o frontend, espelhando o padrão já usado por
// `@/lib/pricing` (re-export de `shared/pricing.ts`).
export * from '../../shared/boostDomain'
